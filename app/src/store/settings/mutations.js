export function APPTitle (state, opened) {
  state.apptitle = opened
}

export function Slogan (state, opened) {
  state.slogan = opened
}

export function Server (state, opened) {
  state.server = opened
}

export function Openid (state, opened) {
  state.openid = opened
}
